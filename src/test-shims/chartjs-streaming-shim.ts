/* Test shim for @aziham/chartjs-plugin-streaming (aliased in vitest.config.ts). A distinct object so
 * a spec asserting the registered plugin union can tell it from the annotation plugin. */
export default { id: 'streaming-shim' };
